
services:
  github-exporter:
    image: mjbelda/github-exporter-dora:1.1.0
    container_name: github-exporter
    hostname: github-exporter
    tty: true
    stdin_open: true
    expose:
      - 9171
    ports:
      - 9171:9171
    environment:
      - REPOS=x-heep/x-heep
      - GITHUB_TOKEN=
    command: ["--web.listen-address=:9171"]
    networks:
      - monitoring

  prometheus:
    image: prom/prometheus:latest
    ports:
      - 9090:9090
    volumes:
      - "./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro"
    depends_on:
      - github-exporter
    networks:
      - monitoring

  grafana:
    image: grafana/grafana-oss:latest
    container_name: grafana
    hostname: grafana
    ports:
      - 3000:3000
    volumes:
      - grafana-storage:/var/lib/grafana
    depends_on:
      - prometheus
    networks:
      - monitoring

networks:
  monitoring:
    driver: bridge

volumes:
  grafana-storage:
